# Auth réelle Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the hardcoded mock session (`lib/auth/index.ts`) with real Supabase Auth for the gérante/staff (`dashboard`/`admin` zones): a login page, a real zone guard in `proxy.ts`, sign-out, and the owner account provisioned in the live database.

**Architecture:** Two Supabase server-client helpers (`lib/supabase/server.ts` for Server Components/Actions via `next/headers` cookies, `lib/supabase/middleware.ts` for `proxy.ts`'s Edge runtime via request/response cookies — Edge middleware cannot use `next/headers`, so these need genuinely different cookie plumbing). A single pure function (`isRoleAllowedForZone`) and a single session-resolution function (`resolveSession`, parameterized over an already-built Supabase client) are shared by both contexts, keeping the actual auth logic in one tested place despite the two client constructors. All authorization decisions use `supabase.auth.getUser()` (server-verified), never `getSession()` (unverified, cookie-only — confirmed via the current `@supabase/ssr` docs before this plan was written).

**Tech Stack:** `@supabase/ssr` (0.12.x), `@supabase/supabase-js` (2.110.x), existing Zod/TypeScript-strict/Server Actions conventions.

## Global Constraints

- TypeScript strict, jamais de `any` (CLAUDE.md §8).
- Server Components par défaut ; `"use client"` seulement si interactivité (CLAUDE.md §8).
- Mutations via Server Actions validées par Zod ; résultats typés (`{ok, data|error}`), jamais d'exception silencieuse (CLAUDE.md §8).
- Toute décision d'autorisation utilise `supabase.auth.getUser()` (vérifié serveur), jamais `supabase.auth.getSession()` (non vérifié) — non-négociable pour ce sous-projet.
- Comptes clients vitrine hors périmètre — aucun changement au checkout KYC (CLAUDE.md §4).
- Pas d'interface super_admin de provisioning de boutique/compte dans ce sous-projet (reporté).
- Node ≥ 22 LTS. `npm run test` / `npm run typecheck` doivent rester verts à chaque tâche (75/75 existants + les nouveaux tests de ce plan).
- Aucune manipulation de mot de passe par un agent — le compte owner est créé manuellement par l'utilisateur (Tâche 7).

---

### Task 1: Dépendances Supabase & clients serveur

**Files:**
- Modify: `package.json`
- Modify: `.env` (local, gitignored)
- Modify: `.env.example`
- Create: `lib/supabase/server.ts`
- Create: `lib/supabase/middleware.ts`

**Interfaces:**
- Produces: `createClient(): Promise<SupabaseClient>` (`lib/supabase/server.ts`, pour Server Components/Actions) et `createMiddlewareClient(request: NextRequest, response: NextResponse): SupabaseClient` (`lib/supabase/middleware.ts`, pour `proxy.ts`). La Tâche 2 consomme les deux.

- [ ] **Step 1: Installer les dépendances**

Run: `npm install @supabase/ssr @supabase/supabase-js`
Expected: `package.json` gagne `"@supabase/ssr": "^0.12.1"` et `"@supabase/supabase-js": "^2.110.3"` (ou la version résolue la plus récente), exit code 0.

- [ ] **Step 2: Ajouter les variables d'environnement (non secrètes)**

Dans `.env` (fichier local, déjà gitignored — ajouter ces deux lignes aux deux placeholders `DATABASE_URL`/`DIRECT_URL` déjà présents depuis le sous-projet 1, ne pas les supprimer) :

```bash
NEXT_PUBLIC_SUPABASE_URL="https://vqqwviknffequjvxmojo.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="sb_publishable_umx7FJFPPfmR9D_CPOPyIw_ASGFZG1r"
```

Dans `.env.example`, ajouter les mêmes deux lignes (elles ne sont **pas secrètes** — clé publique protégée par la RLS, contrairement à `DATABASE_URL`/`DIRECT_URL` qui restent des placeholders génériques) :

```bash
# Supabase — projet réel, valeurs publiques (protégées par RLS), sûres à committer.
NEXT_PUBLIC_SUPABASE_URL="https://vqqwviknffequjvxmojo.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="sb_publishable_umx7FJFPPfmR9D_CPOPyIw_ASGFZG1r"
```

- [ ] **Step 3: Créer `lib/supabase/server.ts`**

```ts
import { createServerClient } from "@supabase/ssr";
import { cookies } from "next/headers";

/**
 * Client Supabase pour Server Components / Server Actions. `setAll` peut
 * échouer silencieusement quand appelé depuis un Server Component pur (lecture
 * seule) — c'est attendu, le rafraîchissement de session est géré par proxy.ts.
 */
export async function createClient() {
  const cookieStore = await cookies();
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll();
        },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) => {
              cookieStore.set(name, value, options);
            });
          } catch {
            // Server Component en lecture seule : sans effet, attendu.
          }
        },
      },
    }
  );
}
```

- [ ] **Step 4: Créer `lib/supabase/middleware.ts`**

```ts
import { createServerClient } from "@supabase/ssr";
import type { NextRequest, NextResponse } from "next/server";

/**
 * Client Supabase pour proxy.ts (Edge runtime — pas d'accès à next/headers,
 * les cookies transitent par NextRequest/NextResponse directement).
 */
export function createMiddlewareClient(request: NextRequest, response: NextResponse) {
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return request.cookies.getAll();
        },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value, options }) => {
            response.cookies.set(name, value, options);
          });
        },
      },
    }
  );
}
```

- [ ] **Step 5: Vérifier que le typecheck passe**

Run: `npm run typecheck`
Expected: aucune sortie, exit code 0.

- [ ] **Step 6: Commit**

```bash
git add package.json package-lock.json .env.example lib/supabase/server.ts lib/supabase/middleware.ts
git commit -m "feat: add Supabase server/middleware client helpers"
```

(`.env` reste gitignored, ne pas le committer.)

---

### Task 2: `lib/auth` — session réelle (TDD)

**Files:**
- Modify: `lib/auth/index.ts`
- Modify: `lib/auth/index.test.ts`

**Interfaces:**
- Consumes: `createClient` (`lib/supabase/server.ts`, Tâche 1).
- Produces: `isRoleAllowedForZone(zone, role): boolean` (pure, exportée), `resolveSession(supabase: SupabaseClient): Promise<Session | null>` (exportée — la Tâche 4/`proxy.ts` la réutilise directement avec le client middleware), `getSession(): Promise<Session | null>`, `requireZone(zone): Promise<{allowed: boolean}>`. `Role` gagne la valeur `"customer"` (aligné sur l'enum Postgres `Role` du sous-projet 1), `Session`/`Zone` inchangés dans leur forme.

- [ ] **Step 1: Écrire les tests (ils doivent échouer avant l'implémentation)**

Remplacer entièrement `lib/auth/index.test.ts` par :

```ts
import { describe, it, expect } from "vitest";
import type { SupabaseClient } from "@supabase/supabase-js";
import { isRoleAllowedForZone, resolveSession } from "@/lib/auth";

function fakeSupabase(
  user: { id: string } | null,
  profile: { role: string; name: string } | null
): SupabaseClient {
  return {
    auth: {
      getUser: async () => ({ data: { user } }),
    },
    from: () => ({
      select: () => ({
        eq: () => ({
          maybeSingle: async () => ({ data: profile }),
        }),
      }),
    }),
  } as unknown as SupabaseClient;
}

describe("isRoleAllowedForZone", () => {
  it("always allows the public storefront zone, even with no role", () => {
    expect(isRoleAllowedForZone("storefront", null)).toBe(true);
  });

  it("allows owner and staff into the dashboard zone", () => {
    expect(isRoleAllowedForZone("dashboard", "owner")).toBe(true);
    expect(isRoleAllowedForZone("dashboard", "staff")).toBe(true);
  });

  it("rejects customer and super_admin from the dashboard zone", () => {
    expect(isRoleAllowedForZone("dashboard", "customer")).toBe(false);
    expect(isRoleAllowedForZone("dashboard", "super_admin")).toBe(false);
  });

  it("only allows super_admin into the admin zone", () => {
    expect(isRoleAllowedForZone("admin", "super_admin")).toBe(true);
    expect(isRoleAllowedForZone("admin", "owner")).toBe(false);
  });

  it("rejects a null role from any privileged zone", () => {
    expect(isRoleAllowedForZone("dashboard", null)).toBe(false);
    expect(isRoleAllowedForZone("admin", null)).toBe(false);
  });
});

describe("resolveSession", () => {
  it("returns null when there is no authenticated user", async () => {
    const session = await resolveSession(fakeSupabase(null, null));
    expect(session).toBeNull();
  });

  it("returns null when the authenticated user has no matching Profile row", async () => {
    const session = await resolveSession(fakeSupabase({ id: "u1" }, null));
    expect(session).toBeNull();
  });

  it("returns the session when both the user and its profile exist", async () => {
    const session = await resolveSession(
      fakeSupabase({ id: "u1" }, { role: "owner", name: "Aïcha Koné" })
    );
    expect(session).toEqual({ userId: "u1", name: "Aïcha Koné", role: "owner" });
  });
});
```

- [ ] **Step 2: Lancer les tests, vérifier qu'ils échouent**

Run: `npx vitest run lib/auth/index.test.ts`
Expected: FAIL — `isRoleAllowedForZone`/`resolveSession` n'existent pas encore dans `lib/auth/index.ts` (erreurs d'import).

- [ ] **Step 3: Réécrire `lib/auth/index.ts`**

```ts
import type { SupabaseClient } from "@supabase/supabase-js";
import { createClient } from "@/lib/supabase/server";

export type Zone = "storefront" | "dashboard" | "admin";
export type Role = "owner" | "staff" | "super_admin" | "customer";

export interface Session {
  userId: string;
  name: string;
  role: Role;
}

const ZONE_ROLES: Record<Exclude<Zone, "storefront">, Role[]> = {
  dashboard: ["owner", "staff"],
  admin: ["super_admin"],
};

/** Pure : aucune dépendance réseau, testable directement. */
export function isRoleAllowedForZone(zone: Zone, role: Role | null): boolean {
  if (zone === "storefront") return true;
  if (!role) return false;
  return ZONE_ROLES[zone].includes(role);
}

/**
 * Résout la session à partir d'un client Supabase déjà construit — factorisé
 * pour être appelable aussi bien depuis un contexte Server Component/Action
 * (lib/supabase/server.ts) que depuis proxy.ts en Edge (lib/supabase/middleware.ts),
 * qui n'ont pas la même API de cookies.
 */
export async function resolveSession(supabase: SupabaseClient): Promise<Session | null> {
  const {
    data: { user },
  } = await supabase.auth.getUser();
  if (!user) return null;

  const { data: profile } = await supabase
    .from("Profile")
    .select("role, name")
    .eq("id", user.id)
    .maybeSingle();
  if (!profile) return null;

  return { userId: user.id, name: profile.name, role: profile.role as Role };
}

/** Convenience Server Component/Action : construit le client puis résout la session. */
export async function getSession(): Promise<Session | null> {
  const supabase = await createClient();
  return resolveSession(supabase);
}

export async function requireZone(zone: Zone): Promise<{ allowed: boolean }> {
  const session = await getSession();
  return { allowed: isRoleAllowedForZone(zone, session?.role ?? null) };
}
```

Note : `getSession()`/`requireZone()` ne sont pas testées directement ici (elles ne font qu'assembler `createClient()` + `resolveSession()` + `isRoleAllowedForZone()`, chacune déjà testée) — cohérent avec le reste du projet, qui ne teste pas les wrappers triviaux.

- [ ] **Step 4: Lancer les tests, vérifier qu'ils passent**

Run: `npx vitest run lib/auth/index.test.ts`
Expected: PASS, 8/8.

- [ ] **Step 5: Lancer la suite complète + typecheck**

Run: `npm run test && npm run typecheck`
Expected: tous les tests passent (75 existants − 3 anciens tests `requireZone` remplacés + 8 nouveaux = 80 au total), aucune sortie de typecheck.

- [ ] **Step 6: Commit**

```bash
git add lib/auth/index.ts lib/auth/index.test.ts
git commit -m "feat: replace the mock session with real Supabase-backed session resolution"
```

---

### Task 3: `lib/validators/auth.ts` — validation du formulaire de connexion (TDD)

**Files:**
- Create: `lib/validators/auth.ts`
- Create: `lib/validators/auth.test.ts`

**Interfaces:**
- Produces: `validateLogin(input: LoginRawInput): {ok: true, data: LoginInput} | {ok: false, errors: LoginFieldErrors}`. La Tâche 5 (`lib/auth/actions.ts`) l'utilise.

- [ ] **Step 1: Écrire le test (doit échouer avant l'implémentation)**

Créer `lib/validators/auth.test.ts` :

```ts
import { describe, it, expect } from "vitest";
import { validateLogin } from "@/lib/validators/auth";

describe("validateLogin", () => {
  it("accepts a valid email and non-empty password", () => {
    const result = validateLogin({ email: "gerante@foulard-teranga.com", password: "un-mot-de-passe" });
    expect(result.ok).toBe(true);
  });

  it("rejects an invalid email", () => {
    const result = validateLogin({ email: "pas-un-email", password: "x" });
    expect(result.ok).toBe(false);
    if (!result.ok) expect(result.errors.email).toBeTruthy();
  });

  it("rejects an empty password", () => {
    const result = validateLogin({ email: "gerante@foulard-teranga.com", password: "" });
    expect(result.ok).toBe(false);
    if (!result.ok) expect(result.errors.password).toBeTruthy();
  });

  it("trims whitespace from the email", () => {
    const result = validateLogin({ email: "  gerante@foulard-teranga.com  ", password: "x" });
    expect(result.ok).toBe(true);
    if (result.ok) expect(result.data.email).toBe("gerante@foulard-teranga.com");
  });
});
```

- [ ] **Step 2: Lancer le test, vérifier qu'il échoue**

Run: `npx vitest run lib/validators/auth.test.ts`
Expected: FAIL — `lib/validators/auth.ts` n'existe pas.

- [ ] **Step 3: Créer `lib/validators/auth.ts`**

```ts
import { z } from "zod";

export const loginSchema = z.object({
  email: z.string().trim().email("Adresse email invalide."),
  password: z.string().min(1, "Mot de passe requis."),
});

export type LoginInput = z.infer<typeof loginSchema>;

export interface LoginFieldErrors {
  email?: string;
  password?: string;
}

export interface LoginRawInput {
  email: string;
  password: string;
}

export function validateLogin(
  input: LoginRawInput
): { ok: true; data: LoginInput } | { ok: false; errors: LoginFieldErrors } {
  const result = loginSchema.safeParse(input);
  if (result.success) return { ok: true, data: result.data };

  const errors: LoginFieldErrors = {};
  for (const issue of result.error.issues) {
    const key = issue.path[0];
    if (key === "email" || key === "password") errors[key] = issue.message;
  }
  return { ok: false, errors };
}
```

- [ ] **Step 4: Lancer le test, vérifier qu'il passe**

Run: `npx vitest run lib/validators/auth.test.ts`
Expected: PASS, 4/4.

- [ ] **Step 5: Commit**

```bash
git add lib/validators/auth.ts lib/validators/auth.test.ts
git commit -m "feat: add login form validation (Zod)"
```

---

### Task 4: `proxy.ts` — garde de zone réelle

**Files:**
- Modify: `lib/proxy/zones.ts`
- Modify: `lib/proxy/zones.test.ts`
- Modify: `proxy.ts`

**Interfaces:**
- Consumes: `resolveSession`, `isRoleAllowedForZone` (`lib/auth`, Tâche 2), `createMiddlewareClient` (`lib/supabase/middleware.ts`, Tâche 1).
- Produces: `proxy.ts` devient `async`. Comportement observable : requête non autorisée sur la zone `dashboard` → redirection `/connexion?next=<chemin d'origine>` ; zone `admin` → redirection `/` inchangée (aucune page de connexion dédiée pour cette zone dans ce sous-projet, cohérent avec le non-goal du spec).

- [ ] **Step 1: Ajouter `/connexion` aux chemins autorisés de la zone dashboard**

Dans `lib/proxy/zones.ts`, modifier :

```ts
export const DASHBOARD_PATHS = [
  "/pos",
  "/tableau-de-bord",
  "/commandes",
  "/inventaire",
  "/clientes",
  "/marketing",
  "/finance",
  "/personnalisation",
] as const;
```

en :

```ts
export const DASHBOARD_PATHS = [
  "/pos",
  "/tableau-de-bord",
  "/commandes",
  "/inventaire",
  "/clientes",
  "/marketing",
  "/finance",
  "/personnalisation",
  "/connexion",
] as const;
```

- [ ] **Step 2: Ajouter un test pour ce chemin**

Dans `lib/proxy/zones.test.ts`, dans le bloc `describe("isPathAllowedForZone", ...)`, ajouter :

```ts
  it("allows the login path in the dashboard zone", () => {
    expect(isPathAllowedForZone("dashboard", "/connexion")).toBe(true);
  });
```

- [ ] **Step 3: Lancer les tests de zones**

Run: `npx vitest run lib/proxy/zones.test.ts`
Expected: PASS (tous les tests existants + le nouveau).

- [ ] **Step 4: Réécrire `proxy.ts`**

```ts
import { NextResponse } from "next/server";
import type { NextRequest } from "next/server";
import { resolveZone, isPathAllowedForZone } from "@/lib/proxy/zones";
import { resolveTenantFromHost } from "@/lib/tenant/registry";
import { resolveSession, isRoleAllowedForZone } from "@/lib/auth";
import { createMiddlewareClient } from "@/lib/supabase/middleware";

export async function proxy(request: NextRequest) {
  const hostname = request.headers.get("host") ?? "localhost";
  const { zone, rewrittenPathname } = resolveZone(hostname, request.nextUrl.pathname);

  if (!isPathAllowedForZone(zone, rewrittenPathname)) {
    return NextResponse.redirect(new URL("/", request.url));
  }

  // Réponse "brouillon" utilisée par le client Supabase pour écrire les cookies
  // de rafraîchissement de session ; recopiée sur la réponse finale plus bas,
  // qu'il s'agisse d'une redirection ou d'un rewrite.
  const authDraft = NextResponse.next();

  if (zone !== "storefront" && rewrittenPathname !== "/connexion") {
    const supabase = createMiddlewareClient(request, authDraft);
    const session = await resolveSession(supabase);
    if (!isRoleAllowedForZone(zone, session?.role ?? null)) {
      // La zone admin (super_admin) n'a pas de page de connexion dédiée dans ce
      // sous-projet (dormant en v1, aucun compte super_admin) — comportement
      // inchangé : redirection vers la vitrine.
      const target = zone === "dashboard" ? "/connexion" : "/";
      const redirectUrl = new URL(target, request.url);
      if (zone === "dashboard") redirectUrl.searchParams.set("next", rewrittenPathname);
      const redirect = NextResponse.redirect(redirectUrl);
      authDraft.cookies.getAll().forEach((cookie) => redirect.cookies.set(cookie));
      return redirect;
    }
  }

  const tenant = resolveTenantFromHost(hostname);
  const requestHeaders = new Headers(request.headers);
  requestHeaders.set("x-tenant-id", tenant.id);

  const url = request.nextUrl.clone();
  url.pathname = rewrittenPathname;

  const rewrite = NextResponse.rewrite(url, {
    request: { headers: requestHeaders },
  });
  authDraft.cookies.getAll().forEach((cookie) => rewrite.cookies.set(cookie));
  return rewrite;
}

export const config = {
  matcher: ["/((?!_next/static|_next/image|favicon.ico).*)"],
};
```

- [ ] **Step 5: Vérifier la suite complète + typecheck**

Run: `npm run test && npm run typecheck`
Expected: tous les tests passent (aucune régression — `proxy.ts` lui-même n'a pas de test dédié, comme avant ce sous-projet ; sa vérification live se fait à la Tâche 8), aucune sortie de typecheck.

- [ ] **Step 6: Commit**

```bash
git add lib/proxy/zones.ts lib/proxy/zones.test.ts proxy.ts
git commit -m "feat: wire proxy.ts to the real session guard, add /connexion redirect"
```

---

### Task 5: Page de connexion + Server Actions

**Files:**
- Create: `lib/auth/actions.ts`
- Create: `app/(auth)/connexion/page.tsx`
- Create: `components/auth/LoginView.tsx`

**Interfaces:**
- Consumes: `createClient` (`lib/supabase/server.ts`, Tâche 1), `validateLogin` (`lib/validators/auth.ts`, Tâche 3).
- Produces: `signIn(prevState, formData): Promise<SignInState>` et `signOut(): Promise<void>` (Server Actions, exportées depuis `lib/auth/actions.ts`) — la Tâche 6 (Sidebar) consomme `signOut` directement.

- [ ] **Step 1: Créer `lib/auth/actions.ts`**

```ts
"use server";

import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
import { validateLogin, type LoginFieldErrors } from "@/lib/validators/auth";

export type SignInState = { ok: false; errors: LoginFieldErrors; formError?: string } | null;

export async function signIn(_prevState: SignInState, formData: FormData): Promise<SignInState> {
  const result = validateLogin({
    email: String(formData.get("email") ?? ""),
    password: String(formData.get("password") ?? ""),
  });
  if (!result.ok) return { ok: false, errors: result.errors };

  const supabase = await createClient();
  const { error } = await supabase.auth.signInWithPassword(result.data);
  if (error) return { ok: false, errors: {}, formError: "Email ou mot de passe incorrect." };

  const next = String(formData.get("next") ?? "/pos");
  redirect(next);
}

export async function signOut(): Promise<void> {
  const supabase = await createClient();
  await supabase.auth.signOut();
  redirect("/connexion");
}
```

Note : `redirect()` (Next.js) lève une exception interne gérée par le framework — ne jamais l'entourer d'un `try/catch` qui l'avalerait. Ici, elle n'est appelée qu'après un `return` anticipé sur l'échec, donc aucun risque.

- [ ] **Step 2: Créer `components/auth/LoginView.tsx`**

```tsx
"use client";

import { useActionState } from "react";
import { useSearchParams } from "next/navigation";
import { signIn, type SignInState } from "@/lib/auth/actions";
import { colors, fonts } from "@/lib/theme/tokens";

export function LoginView() {
  const searchParams = useSearchParams();
  const next = searchParams.get("next") ?? "/pos";
  const [state, formAction, pending] = useActionState<SignInState, FormData>(signIn, null);

  return (
    <div style={{ maxWidth: 380, margin: "96px auto", padding: "0 16px" }}>
      <h1 style={{ fontFamily: fonts.display, fontSize: 24, fontWeight: 600, marginBottom: 24 }}>
        Connexion
      </h1>
      <form action={formAction} style={{ display: "flex", flexDirection: "column", gap: 16 }}>
        <input type="hidden" name="next" value={next} />
        <label style={{ display: "flex", flexDirection: "column", gap: 6, fontSize: 13, fontWeight: 500 }}>
          Email
          <input
            type="email"
            name="email"
            required
            autoComplete="email"
            style={{ padding: "10px 12px", borderRadius: 8, border: `1px solid ${colors.borderField}` }}
          />
          {state && !state.ok && state.errors.email && (
            <span style={{ color: colors.danger, fontSize: 12 }}>{state.errors.email}</span>
          )}
        </label>
        <label style={{ display: "flex", flexDirection: "column", gap: 6, fontSize: 13, fontWeight: 500 }}>
          Mot de passe
          <input
            type="password"
            name="password"
            required
            autoComplete="current-password"
            style={{ padding: "10px 12px", borderRadius: 8, border: `1px solid ${colors.borderField}` }}
          />
          {state && !state.ok && state.errors.password && (
            <span style={{ color: colors.danger, fontSize: 12 }}>{state.errors.password}</span>
          )}
        </label>
        {state && !state.ok && state.formError && (
          <p style={{ color: colors.danger, fontSize: 13 }}>{state.formError}</p>
        )}
        <button
          type="submit"
          disabled={pending}
          style={{
            padding: "11px 16px",
            borderRadius: 8,
            border: "none",
            background: colors.primary,
            color: "#fff",
            fontWeight: 600,
            cursor: pending ? "default" : "pointer",
            opacity: pending ? 0.7 : 1,
          }}
        >
          {pending ? "Connexion…" : "Se connecter"}
        </button>
      </form>
    </div>
  );
}
```

- [ ] **Step 3: Créer `app/(auth)/connexion/page.tsx`**

Ce groupe de routes `(auth)` est délibérément séparé de `(dashboard)` : il **n'hérite pas** de `app/(dashboard)/layout.tsx` (Sidebar/TopBar), qui n'aurait aucun sens pour un visiteur pas encore authentifié — il hérite seulement du layout racine (`app/layout.tsx`). `(auth)` étant un groupe de routes (parenthèses), il n'apparaît pas dans l'URL : ce fichier sert bien `/connexion`.

```tsx
import { Suspense } from "react";
import { LoginView } from "@/components/auth/LoginView";

export default function ConnexionPage() {
  return (
    <Suspense fallback={<div style={{ maxWidth: 380, margin: "96px auto" }} />}>
      <LoginView />
    </Suspense>
  );
}
```

(`Suspense` requis : `useSearchParams()` dans `LoginView` l'exige en App Router — même pattern que `app/(storefront)/confirmation/page.tsx`.)

- [ ] **Step 4: Vérifier la suite complète + typecheck**

Run: `npm run test && npm run typecheck`
Expected: tous les tests passent (aucun nouveau test dans cette tâche — UI + Server Actions non testées unitairement, cohérent avec le reste du projet ; vérification live à la Tâche 8), aucune sortie de typecheck.

- [ ] **Step 5: Commit**

```bash
git add lib/auth/actions.ts "app/(auth)/connexion/page.tsx" components/auth/LoginView.tsx
git commit -m "feat: add the real login page and signIn/signOut Server Actions"
```

---

### Task 6: Session réelle dans le Sidebar (remplace le nom en dur) + déconnexion

**Files:**
- Modify: `app/(dashboard)/layout.tsx`
- Modify: `components/dashboard/Sidebar.tsx`

**Interfaces:**
- Consumes: `getSession` (`lib/auth`, Tâche 2), `signOut` (`lib/auth/actions.ts`, Tâche 5), `initials` (`lib/format.ts`, déjà existant).

- [ ] **Step 1: Rendre `app/(dashboard)/layout.tsx` async et lui faire résoudre la session**

Remplacer le contenu de `app/(dashboard)/layout.tsx` par :

```tsx
import { Sidebar } from "@/components/dashboard/Sidebar";
import { TopBar } from "@/components/dashboard/TopBar";
import { MobileNav } from "@/components/dashboard/MobileNav";
import { OfflineBanner } from "@/components/dashboard/OfflineBanner";
import { Toast } from "@/components/dashboard/Toast";
import { TicketModal } from "@/components/dashboard/TicketModal";
import { getSession } from "@/lib/auth";

export default async function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const session = await getSession();

  return (
    <div
      style={{
        minHeight: "100vh",
        display: "flex",
        background: "var(--color-ivory)",
        color: "var(--color-ink)",
      }}
    >
      <Sidebar session={session} />

      <div style={{ flex: 1, minWidth: 0, display: "flex", flexDirection: "column" }}>
        <OfflineBanner />
        <TopBar />
        <main className="ft-main" style={{ flex: 1, minWidth: 0, overflowX: "hidden" }}>
          {children}
        </main>
        <MobileNav />
      </div>

      <Toast />
      <TicketModal />
    </div>
  );
}
```

Note : `proxy.ts` (Tâche 4) a déjà refusé l'accès à cette zone si la session n'est pas valide — `session` ne devrait donc jamais être `null` en pratique ici. `Sidebar` gère quand même ce cas défensivement (ci-dessous), pour ne jamais planter sur une race condition théorique plutôt que de présumer l'invariant.

- [ ] **Step 2: Adapter `components/dashboard/Sidebar.tsx`**

Ajouter l'import en haut du fichier, avec les autres imports :

```ts
import { initials } from "@/lib/format";
import { signOut } from "@/lib/auth/actions";
import type { Session } from "@/lib/auth";
```

Changer la signature du composant de :

```ts
export function Sidebar() {
```

à :

```ts
const ROLE_LABELS: Record<Session["role"], string> = {
  owner: "Gérante",
  staff: "Staff",
  super_admin: "Super admin",
  customer: "Cliente",
};

export function Sidebar({ session }: { session: Session | null }) {
```

Remplacer le bloc final (lignes 159-191 du fichier actuel, le bloc `<div style={{ display: "flex", alignItems: "center", gap: 10, padding: "10px 10px 4px" }}>...</div>` qui affiche en dur "AK" / "Aya Koffi" / "Gérante") par :

```tsx
        <div style={{ display: "flex", alignItems: "center", gap: 10, padding: "10px 10px 4px" }}>
          <span
            style={{
              width: 34,
              height: 34,
              borderRadius: 999,
              background: colors.primary,
              display: "flex",
              alignItems: "center",
              justifyContent: "center",
              fontWeight: 600,
              fontSize: 13,
              color: "#fff",
              flex: "none",
            }}
          >
            {session ? initials(session.name) : "?"}
          </span>
          <div style={{ lineHeight: 1.2, minWidth: 0 }}>
            <div
              style={{
                fontSize: 13,
                fontWeight: 600,
                whiteSpace: "nowrap",
                overflow: "hidden",
                textOverflow: "ellipsis",
              }}
            >
              {session?.name ?? "Session expirée"}
            </div>
            <div style={{ fontSize: 11, color: colors.navSub }}>
              {session ? ROLE_LABELS[session.role] : ""}
            </div>
          </div>
        </div>
        <form action={signOut} style={{ padding: "4px 10px 0" }}>
          <button
            type="submit"
            style={{
              width: "100%",
              padding: "8px 10px",
              borderRadius: 10,
              border: "none",
              background: "transparent",
              color: colors.navIdle,
              fontSize: 12.5,
              fontWeight: 500,
              textAlign: "left",
              cursor: "pointer",
            }}
          >
            Se déconnecter
          </button>
        </form>
```

- [ ] **Step 3: Vérifier la suite complète + typecheck**

Run: `npm run test && npm run typecheck`
Expected: tous les tests passent, aucune sortie de typecheck.

- [ ] **Step 4: Commit**

```bash
git add "app/(dashboard)/layout.tsx" components/dashboard/Sidebar.tsx
git commit -m "feat: show the real signed-in user in the Sidebar, add sign-out"
```

---

### Task 7: Provisionner le compte owner (étape humaine requise)

**Files:**
- Create: `prisma/migrations/<timestamp>_seed_owner_profile/migration.sql` (nom exact déterminé à l'exécution — voir Step 4)

**Interfaces:**
- Consumes: aucune (opération manuelle sur la base live).
- Produces: une ligne `Profile` réelle (`role = 'owner'`), permettant à la Tâche 8 de vérifier le parcours de connexion de bout en bout.

**⚠️ Cette tâche ne peut pas être déléguée à un sous-agent implémenteur de bout en bout — elle nécessite une action humaine (créer le compte) entre les étapes 1 et 2. Quiconque exécute ce plan doit s'arrêter à l'étape 1 et attendre la réponse de l'utilisateur avant de continuer.**

- [ ] **Step 1: STOP — demander à l'utilisateur**

Demander à l'utilisateur :
1. De créer le compte owner dans le dashboard Supabase (Authentication > Users > Add user), avec l'email et le mot de passe de son choix.
2. De fournir : (a) l'email utilisé, (b) le nom complet à afficher (ex. « Aïcha Koné », ou son vrai nom).

Ne pas continuer avant d'avoir ces deux informations.

- [ ] **Step 2: Retrouver l'id du compte créé (lecture seule)**

Appeler `mcp__supabase__execute_sql` avec :

```sql
select id, email from auth.users where email = '<email fourni par l'utilisateur>';
```

Expected : exactement une ligne. Noter l'`id` (uuid).

- [ ] **Step 3: Créer la ligne Profile correspondante**

Écrire le fichier `prisma/migrations/<timestamp>_seed_owner_profile/migration.sql` (timestamp au format `YYYYMMDDHHMMSS`, postérieur aux deux migrations du sous-projet 1) avec le contenu suivant (remplacer `<uuid>` et `<nom>` par les valeurs réelles trouvées/fournies — échapper les apostrophes françaises éventuelles du nom en les doublant, ex. `N''Guessan`, comme dans `prisma/seed.sql`) :

```sql
insert into "Profile" (id, "tenantId", role, name)
values ('<uuid>', 'foulard-teranga', 'owner', '<nom>');
```

Appeler `mcp__supabase__execute_sql` avec cette requête exacte.

- [ ] **Step 4: Vérifier**

Appeler `mcp__supabase__execute_sql` avec :

```sql
select id, "tenantId", role, name from "Profile" where id = '<uuid>';
```

Expected : une ligne, `role = 'owner'`, `"tenantId" = 'foulard-teranga'`, `name` = le nom fourni.

- [ ] **Step 5: Commit**

```bash
git add prisma/migrations/
git commit -m "feat: provision the owner Profile row for the real Supabase Auth account"
```

---

### Task 8: Vérification finale, checklist manuelle & docs

**Files:**
- Modify: `docs/superpowers/EXECUTION-STATUS.md`

- [ ] **Step 1: Suite complète**

Run: `npm run test`
Expected : tous les tests passent (baseline 75 + 8 nouveaux `lib/auth` + 4 nouveaux `lib/validators/auth` + 1 nouveau `zones` − 3 anciens tests `requireZone` remplacés = 85).

Run: `npm run typecheck`
Expected : aucune sortie, exit code 0.

- [ ] **Step 2: Écrire la checklist de parcours manuel**

Ajouter à `docs/superpowers/EXECUTION-STATUS.md`, dans une nouvelle section après celle du sous-projet 1 :

```markdown
## Migration mock → Supabase : sous-projet 2/5 (Auth réelle)

**Automatisé : fait et vert.** `npm run test` / `npm run typecheck` passent (voir ci-dessus). Compte owner provisionné (Tâche 7).

**Parcours manuel à vérifier en navigateur (non exécuté par l'agent — outils navigateur indisponibles dans cette session, cf. sous-projet vitrine) :**

1. Ouvrir `/admin/pos` sans être connecté → redirection vers `/admin/connexion?next=%2Fpos`, sans boucle.
2. Se connecter avec le compte owner provisionné → redirection vers `/pos`, Sidebar affiche le vrai nom + « Gérante ».
3. Naviguer dans `/admin/commandes`, `/admin/inventaire`, etc. → accès direct, pas de redemande de connexion.
4. Cliquer « Se déconnecter » → retour à `/admin/connexion` ; un accès direct à `/admin/pos` redemande une connexion.
5. Recharger la page après connexion → session toujours active (cookies persistés).
6. Tenter `/platform/boutiques` avec le compte owner connecté → toujours refusé (rôle `owner` ≠ `super_admin`), redirection vers `/`.
7. Mauvais mot de passe → message d'erreur inline, pas de crash, pas de redirection.

Si un problème est trouvé : corriger, ré-exécuter l'étape concernée, committer (`git commit -m "fix: ..."`).
```

- [ ] **Step 3: Commit**

```bash
git add docs/superpowers/EXECUTION-STATUS.md
git commit -m "docs: record real-auth sub-project status and manual verification checklist"
```

## Self-Review Notes

- **Spec coverage :** §2 (périmètre owner/staff, provisioning manuel, pas d'UI super_admin) → Tâches 5/7 et non-goals respectés partout ; §3.2 (réécriture `lib/auth`) → Tâche 2 ; §3.3 (`proxy.ts`) → Tâche 4 ; §3.4/3.5 (page de connexion, déconnexion) → Tâches 5/6 ; §3.6 (zone admin dormante) → Tâche 4 Step 4 ; §4 (deps/env) → Tâche 1 ; §5 (provisioning) → Tâche 7 ; §6 (tests) → Tâches 2/3 (TDD) + Tâche 8 (checklist manuelle) ; §7 (non-goals) → aucune tâche ne les enfreint ; §8 (critères de réussite) → Tâche 8 Step 2.
- **Déviation notée par rapport au pseudocode du spec (§3.2/3.3) :** le spec montrait `getSession()`/`requireZone()` appelées directement depuis `proxy.ts`. En pratique, le middleware Edge (`proxy.ts`) ne peut pas utiliser `next/headers` (réservé aux Server Components/Actions) — d'où la factorisation en `resolveSession(supabase)`/`isRoleAllowedForZone()`, pures et injectables, appelées avec le client adapté à chaque contexte. Le comportement observable (RLS, `getUser()` vérifié, cibles de redirection) reste identique à ce qui a été approuvé ; seule la plomberie interne diffère de l'esquisse du spec.
- **Placeholder scan :** aucun TBD/TODO ; chaque étape de code contient le contenu complet.
- **Type consistency :** `Session`, `Role`, `Zone` définis une fois (Tâche 2) et réutilisés à l'identique dans `proxy.ts` (Tâche 4), `Sidebar.tsx`/`layout.tsx` (Tâche 6) et `lib/auth/actions.ts` (Tâche 5, indirectement via `createClient`).
